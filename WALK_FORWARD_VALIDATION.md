# Walk-forward 样本外验证报告

> 生成脚本：`scripts/walk_forward_validator.py --sweep`
> 数据区间：23 个历史信号日（2026-06-10 → 2026-07-31，有效 21 天）
> 口径：**100% 持仓、连续再投资、无现金缓冲**（与生产回测「各 sleeve 独立 + 含 70% 现金」口径不同，相对结论一致）

## 方法（严格因果，无未来函数）

对每个信号日 `Ti`：

1. **因果权重**：只用「严格早于 `Ti`」的历史回测 trades 计算各策略 edge
   （`compute_strategy_edge` 的 cutoff 版），再经 `adaptive_weights()` 得到当天权重。
   与生产运行时一致——权重由过去决定，收益用未来检验。
2. **同票对比**：在 `Ti` 的同一批候选票上，对比两种组合的前向收益
   （`return_pct` 已是次日开盘买入、T+1、含真实佣/税/滑点的已实现收益）：
   - **自适应组合**：每票按「命中策略的因果权重取 max」分配；
   - **等权组合**：每票 `1/N`。
   两者只在权重分配这一步不同，差异纯粹反映权重决策是否增值。
3. **样本外单调性**：把所有票按 `Ti` 当天因果权重分高/中/低三档，看高权重档
   前向收益是否真的高于低权重档——这是「自适应信号是否灵」的核心检验。

纯读历史产物，零网络、零新回测、不改生产代码。

## 核心结论

### 1. 自适应加权在组合层面 ≈ 等权（几乎零增益）

| 指标 | 自适应 | 等权 |
|---|---|---|
| 累计收益 | **-49.55%** | -49.02% |
| 差值 | \multicolumn{2}{c}{-0.53%（自适应略差）} |
| 胜日率 | 33.3%（7/21） | 38.1%（8/21） |

→ **当前的自适应加权几乎没带来超额收益**，等权还多赢 1 天。

### 2. 但底层 edge 信号在「个股层面」确实灵（单调性成立）

| 权重档 | n | 均值前向收益 | 胜率 |
|---|---|---|---|
| 低权重档 | 22 | **-2.722%** | 63.6% |
| 中权重档 | 22 | -2.683% | 45.5% |
| 高权重档 | 23 | **-0.176%** | 52.2% |

→ 高权重档（-0.18%）远优于低权重档（-2.7%），**单调、差距 2.5pp**。
说明 `compute_strategy_edge` 正确识别了「哪些策略的票会更好」。

### 3. 矛盾的根源：平滑把 edge 稀释没了

第 2 点证明信号有效，第 1 点却显示组合没吃到——因为
`shrinkage=0.4`（向等权拉 40%）+ `FLOOR=3.0`（亏损策略保底 3% 权重）
把权重抹得太平，组合几乎退化成等权，吃不到个股层面的信号。

## 参数敏感性扫描（放松平滑/地板后的样本外表现）

| 配置 | 自适应 | 等权 | 差值 |
|---|---|---|---|
| 生产默认 (shrinkage=0.4, FLOOR=3) | -49.55% | -49.02% | -0.52 |
| 中等平滑 (shrinkage=0.2) | -52.10% | -49.02% | -3.07 |
| 无收缩 (shrinkage=0) | -50.24% | -49.02% | -1.22 |
| 去地板+适度平滑 (shrinkage=0.2, FLOOR=0) | -52.10% | -49.02% | -3.07 |
| **无收缩+无地板 (shrinkage=0, FLOOR=0)** | **-43.67%** | -49.02% | **+5.35** |

**关键发现**：唯一跑赢等权的配置是「彻底去掉向等权的拉力 + 去掉 FLOOR」。
拖累主要来自 **FLOOR=3.0**——它让亏损策略一直拿底仓，稀释了好策略。
（注：`shrinkage=0.2` 时地板有无都一样，因为该拉力已把权重压平、根本碰不到地板；
地板只在 `shrinkage=0` 权重变极端时才起作用。）

## 解读与建议

- **FLOOR 是过度保险**：当初加 FLOOR 是为防「单策略被清零→幸存票爆雷」。
  但那个坑现在已由 **每策略上限 10 + 单票仓位上限** 解决，FLOOR 已无必要、
  反而在持续资助亏损策略、拖累收益。
- **建议的方向**（需前向验证后再改生产）：保留样本贝叶斯收缩（`pseudo=15`），
  去掉向等权的 `shrinkage` 拉力和 `FLOOR`，让权重纯由 edge 驱动。
  扫描显示此举潜在可把组合相对等权提升约 **+5pp**。
- **风险提示**：以上基于单一熊市区间（21 天）的 walk-forward，存在过拟合该周期的风险。
  改生产默认参数前，应先做一段前向纸面跟踪（或扩大样本区间）确认稳健，
  且当前 `max_per_strategy=10` + 单票仓位上限已能防单票爆雷，去 FLOOR 不会 reintroduce 该风险。

## 复现

```bash
/e/Anaconda/python.exe scripts/walk_forward_validator.py --sweep
```
