# Hugging Face Spaces 依赖
fastapi>=0.115
uvicorn>=0.30
pandas
akshare
baostock
pytdx>=1.72  # 优先级最高的 K线/行情数据源（直连券商，毫秒级）；CI 不可达时自动回退 akshare
matplotlib
pillow
requests
python-dotenv>=1.0
supabase>=2.0  # optional: cloud trade sync (TRADES_BACKEND=supabase)
