# 2026-07-09 工作日志

## 多策略 Backtrader 回测引擎（实现完成）
- 用户要求："实现 不要单独做boll的 现在开始所有都是按多策略的做"
- 把旧 `smcore/backtest.py`（单文件 signal 回测）升级为**包** `smcore/backtest/`：
  - `__init__.py`：保留 `run_signal_backtest` + 新增 `run_multi_strategy_backtest`
  - `signal_backtest.py`：原 hold-N-days 回测（兼容旧接口）
  - `loader.py`：数据适配（个股复用 `fetch_daily_k`；指数 `ak.stock_zh_index_daily` 用于 Relativity，拉不到优雅降级）
  - `strategies.py`：核心 `MultiStrategy(bt.Strategy)` + `PriceData`（含成交额 line）+ `CNCommInfo`（佣金万2.5最低5 + 卖出印花税千0.5）
  - `engine.py`：`run_multi_strategy_backtest` 组装 Cerebro，返回与旧接口同构的 `BacktestResult(summary/equity/trades)`
- 四策略融合口径**照搬 fusion.py**：boll40/relativity25/theme20/cctv15 加权，单票仓位 min(命中策略最高权重/100, 0.3)；止损=布林下轨/止盈=布林上轨
- Boll/Relativity/Theme 纯行情可跑；CCTV 是新闻因子，需外部 `cctv_hits` 注入（默认关闭）
- 后端：`/api/backtests/run` 新增 `mode:"multi"` 分支（start/end/strategies），复用现有轮询+展示；顺手修复原文件缺 `timedelta` 导入的潜在 NameError
- 前端：回测 Tab 新增"手动多策略回测"表单（代码/区间/策略多选）→ 复用 backtestRun 展示
- `requirements.txt` 加 `backtrader>=1.9.78`
- 验证：合成数据全链路通过（14笔交易、胜率35.7%、总收益+31.44%、夏普2.99、回撤-3.83%，boll/theme 双命中、佣金生效、relativity 激活）；akshare `stock_zh_a_daily` 沙箱可用(0.9s)→真实数据路径畅通

## 关键坑
- Backtrader 1.9.78 的 `Trade`：**平仓后 `trade.size` 归零且 `trade.history` 为空**，真实手数/价格必须用 `notify_order` 的 `order.executed.size/price` 记录
- `CommInfoBase` 无 `COMMISSION_PERC` 类属性 → 移除该 param，重写 `getcommission` 即可
- 沙箱 pip 安装触发"安全删除(回收站不可用)"失败 → 重定向 `PIP_CACHE_DIR`/`TMP` 到受管目录 + `--no-cache-dir` + `--target` 解决
- 受管 venv 已装 backtrader/pandas/numpy/akshare（隔离，不污染 Anaconda）

## 本地运行环境坑（重要）
- 本地 `stocks-master.bat` 后端用 **Anaconda** 的 `E:\Anaconda\python.exe app.py`（非受管 venv）——app.py 做 `from backend.main import app` → 会 import `smcore.backtest` → 需要 `backtrader`
- 因此 backtrader **必须装进 Anaconda**（`E:\Anaconda\python.exe -m pip install backtrader`，已装 1.9.78.123）。受管 venv 里的 backtrader 只用于我的验证，本地 app 用不到
- 若上传 Render：buildCommand 跑 `pip install -r requirements.txt`（已含 backtrader），自动带上

## 部署平台定档：Oracle Always Free（用户"用oracle"）
- 用户决策："用oracle"（替代 Render）。节点选东京 ap-tokyo-1 / 首尔 ap-seoul-1（离中近、akshare 稳、免费永不停）
- 新建文件：
  - `Dockerfile`：多阶段（node:22-slim 构建 frontend/dist → python:3.11-slim 装依赖跑 uvicorn）；`.dockerignore` 排除 node_modules/stock_data/Frequently-Used-Program/.git 等
  - `docker-compose.yml`：restart always、8000、env(TDX_ENABLED=0/KLINE_BACKEND=akshare/TZ=Asia/Shanghai)、挂载 `./stock_data:/app/stock_data:ro`、healthcheck
  - `.github/workflows/deploy.yml`：push 到 master(排除"chore: 预热看板缓存"数据提交) → SSH 进 VM 重建镜像；schedule 13:45 UTC 工作日 → 仅 git pull 同步数据。用 appleboy/ssh-action + secrets(ORACLE_HOST/USER/SSH_KEY)
  - `DEPLOY_ORACLE.md`：开实例/安全列表/装 docker/deploy key/clone/Caddy 可选/自动部署全流程
- 验证：backend+backtest py_compile 通过；frontend/dist/index.html 存在；deploy.yml + docker-compose.yml YAML 合法；Dockerfile 的 frontend/dist 路径与 main.py `FRONTEND_DIST=ROOT/frontend/dist` 对齐
- 注意：Oracle 东京仍海外 → TDX_ENABLED=0 不变；render.yaml 保留兜底但主链路已切 Oracle（需手动断 Render 避免双部署）
- 所有改动（含之前市场宽度/表格/多策略回测）**仍未 commit/push**

## 部署平台转向（无信用卡 → Oracle 排除）
- 用户告知"没有信用卡"：Oracle Cloud Always Free 注册必须国际信用卡验证，**Oracle 方案作废**。
- 待与用户确认新方案（HuggingFace Spaces Docker / Cloudflare Pages 纯静态 / Render Free / 国内云他法支付）。
- 已落地的 Oracle 配置（Dockerfile/compose/deploy.yml/DEPLOY_ORACLE.md）暂保留参考，等定档后处理。

## 定档：Cloudflare Pages 纯静态（用户选此项，免信用卡·国内快）
- 用户在无信用卡前提下选 **Cloudflare Pages 纯静态**（替代后端常驻）。核心改造：
  - `scripts/generate_static_data.py`：复用 smcore 把看板/每日清单/回测/选股候选/个股分析/task-logs 全部预生成为 `frontend/public/data/api/*.json`（Vite 构建自动拷到 `dist/data/api`）。
  - `frontend/src/staticShim.js` + 改 `main.jsx`：monkey-patch `window.fetch`，GET `/api/X` → `/data/api/X.json`；POST 回测/选股返回固定 task_id 指预生成结果；`/api/portfolio`、`/api/trades` 改 localStorage。**App.jsx 业务逻辑零改动**。VITE_USE_BACKEND=1 可切回连本地后端调试。
  - `frontend/public/functions/[[path]].js`：Cloudflare Pages SPA fallback（未命中静态资源回退 index.html）。
  - `wrangler.toml`：`name=stocks-master`，`pages_build_output_dir=frontend/dist`。
  - `.github/workflows/deploy-cf.yml`：push master 触发 `wrangler pages deploy frontend/dist --project-name=stocks-master`；**已加构建兜底**（frontend/dist 不存在时现场 generate+build），首次推送也能成功；需要 Secrets `CF_API_TOKEN`/`CF_ACCOUNT_ID`。
  - `.github/workflows/daily-pick.yml`：加装 backtrader + 新增"生成静态 API 数据"步骤 + 构建 + 提交 `frontend/dist` 与 `frontend/public/data/api` 回 master（触发 deploy-cf 自动更新）。
  - `.gitignore`：加忽略前端产物（Actions 用 `git add -f` 强制提交）。
  - `DEPLOY_CLOUDFLARE.md`：完整免费/免信用卡/国内访问说明 + 手动部署步骤 + 功能降级表。
- 验证：PY_COMPILE_OK；`vite build` 成功且 `dist/functions/[[path]].js` 正确复制；smcore import 链本地可导入（受管 venv 已补 requests/supabase/cos-sdk/baostock/matplotlib）。端到端实跑 generate_static_data.py 后台验证中。
- **用户待办**：①注册 Cloudflare(GitHub/Google登录免卡) ②拿 Account ID ③建 Pages:Edit API Token ④GitHub 仓库加 Secrets `CF_API_TOKEN`/`CF_ACCOUNT_ID` ⑤批准我 commit+push 到 master 触发首次部署。
- 已知取舍（已写进 DEPLOY_CLOUDFLARE.md）：组合管理走 localStorage 不跨设备；回测/选股为 Actions 预生成固定结果（前端输入参数被忽略）；个股分析仅对每日清单内股票预生成。
- **额度澄清（用户担心构建次数不够）**：我们用 `wrangler pages deploy` 是"直接上传预构建产物"，Cloudflare **不跑构建** → 其 500 次/月构建额度 **0 消耗、部署无限**。真正约束是 **GitHub Actions 分钟**：私库 2000/月、公库无限；daily-pick 工作日~22次×30–45min≈700–1000min/月，余量充足。可选优化：仓库设 Public（Actions 无限）/ 降频 / paths-ignore 已屏蔽非必要部署。

## 部署平台再定档：Render（用户"算了 render也能用"）
- 用户放弃 Cloudflare 纯静态，回到 **Render Free 完整后端**。改动：
  - `frontend/src/main.jsx`：拦截层改为**默认连真实后端**，仅 `VITE_STATIC=1` 才启用 `staticShim.js`（Cloudflare 备选）。
  - `render.yaml`：补 `TDX_ENABLED=0`、`healthCheckPath: /`、`branch: master`；保留原生 python 运行时 + uvicorn。
  - `.github/workflows/daily-pick.yml`：删掉"生成静态数据 + 构建前端 + 提交 dist"步骤，只生成/提交 `stock_data`（Daily-Action-List + daily_cache）→ push 触发 Render 自动重新部署。
  - 删除 Cloudflare 专属文件：`deploy-cf.yml`、`wrangler.toml`、`frontend/public/functions/[[path]].js`。
  - 新增 `DEPLOY_RENDER.md`（主文档）；`DEPLOY_CLOUDFLARE.md` 降级为可选备选说明。
  - `keep-alive.yml` 已存在（每 10min ping `RENDER_URL` secret 保活免费实例）—部署时需配该 secret。
- 待办：用户连 Render 仓库 + 配 `RENDER_URL`/`SUPABASE_*` + 批准 commit+push 触发首次部署。

## 最终清理 + auto-boll 迁入（2026-07-09 末）
- 用户指令："把不要的删掉 然后把我的auto-boll的策略搬过来"。
- 删除的"不要的"文件（均为未跟踪/历史残留，安全删）：`Dockerfile`、`docker-compose.yml`、`.dockerignore`、`.github/workflows/deploy.yml`、`DEPLOY_CLOUDFLARE.md`、`DEPLOY_ORACLE.md`、`DEPLOY_CLOUD.md`、`frontend/src/staticShim.js`、`scripts/generate_static_data.py`、`frontend/public/data/`；`git rm --cached frontend/dist`（Render 自构建不跟踪）。
- 前端 `main.jsx` 还原纯净版（移除 staticShim import，避免指向已删文件构建报错）。
- auto-boll 重构成 `smcore/strategies/boll_selection.py` 的 `run_boll_selection()`（完整多因子逻辑：资金流向→基本面→剔除创业/科创→重要流通股东→布林超卖/近下轨），`__init__.py` 导出；布林统一走 `smcore.indicators.boll`，K线 baostock 前复权为主+akshare 兜底。
- `Frequently-Used-Program/Stock-Selection-Boll.py` 改 3 行 shim 调用真实实现（`auto_notify_boll.py`/`daily-pick.yml` 兼容）。
- `smcore/selection.py`：空候选时 `scan_boll_batch([])` 调 `run_boll_selection()` 返回归一化列；前端选股面板新增"运行完整布林选股(多因子)"按钮 → `POST /api/selection/boll-scan` 空 body 触发。
- 验证：`py_compile` OK；Anaconda `import smcore.strategies.boll_selection` + `run_boll_selection` 存在 OK；`vite build` 成功(1792 modules)。
- 注意：git status 出现大量 `stock_data/cache/k_data/*.csv`（验证跑出来的缓存）被跟踪/修改——提交时应按需 `git add` 策略与源码、避免把缓存噪声全 push（或补 `.gitignore` 忽略 `stock_data/cache`）。
- 所有改动仍未 commit/push（默认分支 master）。

## 完成：commit + push（2026-07-09 末）
- 用户"继续"→ 按推荐执行：`.gitignore` 加 `stock_data/cache/` 并 `git rm --cached -r stock_data/cache` 取消运行时缓存跟踪；`frontend/dist` 取消跟踪（Render 自构建）。
- 显式 stage 项目文件（未用 `git add -A`，避免混入 `.workbuddy` 记忆与缓存噪声）。注意：Dockerfile/compose/deploy.yml/各 DEPLOY_*.md/staticShim/generate_static_data 此前均为未跟踪文件，删盘即无需 git 动作，仅 `DEPLOY_CLOUD.md` 是已跟踪需 stage 删除。
- commit `a31fcd5`（714 files, +633/-59721）：refactor 清理 + auto-boll 重构为 smcore 模块。`.workbuddy` 正确排除在提交外。
- `git push origin master` 成功（`b9489d1..a31fcd5`）→ 触发 Render 自动部署。
- 待用户：连 Render 仓库确认 Web Service 部署成功 + 配 `RENDER_URL`/`SUPABASE_*` secret（keep-alive 保活用）。

## 重命名 + 删冗余（2026-07-09 深夜）
- 用户指令："改个名字 这是多策略的 删去没用的"。改名目标=boll_selection 模块/函数→多策略统一风格；删除范围=用户授权"你看着办"。
- 改名：`smcore/strategies/boll_selection.py` → `boll.py`，`run_boll_selection` → `run_boll`（与后续 Theme/Relativity/CCTV 并列）。更新 `smcore/strategies/__init__.py`、`smcore/selection.py`、shim；`boll.py` 加 `if __name__=="__main__": run_boll()`；CSV 输出名 `Stock-Selection-Boll-{date}.csv` 保持不变（融合/auto_notify 按此 glob）。
- 删除"没用的"：`Frequently-Used-Program/Stock-Selection-Boll.py`（shim 已无引用）与 `Stock-Selection-Boll-All.py`（冗余批量变体；仅 archive/cleanup 正则引用其历史 CSV 输出，保留正则）。`strategy_common` 仍被 `Stock-Selection-Relativity.py` 需要，保留。其余 3 策略独立脚本(Theme/Relativity/CCTV)未迁移故保留。
- 调用方改写：`auto_notify_boll.py` 的 boll 步骤 `cmd=[sys.executable, "-m", "smcore.strategies.boll"]`（cwd=ROOT_DIR，可解析）；`daily-pick.yml` 同改。同步修正 smcore 内历史注释 + Readme/子项目 README。
- 踩坑：先 `git mv` 后编辑 boll.py，但 `git add` 列表漏了 boll.py → 首提 `4ed2a6a` 含旧 `run_boll_selection`（会 import 失败）。push 未成，发现后 `git add smcore/strategies/boll.py` + `git commit --amend` 修正为 `0016632`（未 push 前 amend 安全）。再 push 成功（`a31fcd5..0016632`）。
- 验证：`py_compile` OK；venv `import smcore.strategies.boll` + `run_boll`/`scan_boll_batch` 存在 OK。

## 四策略全迁入 smcore（2026-07-09 深夜续）
- 用户"可以"→ 按 boll.py 套路把 **Theme/Relativity/CCTV** 也迁入 `smcore/strategies/`，并清掉 `Frequently-Used-Program/` 下全部独立选股脚本。
- `git mv` 三个脚本为 `smcore/strategies/{theme,relativity,cctv}.py`；各模块 `ROOT_DIR` 改指向 `smcore.config.defaults.PROJECT_ROOT`（不再 `parents[1]`）；`main()`→`run_*(`，加 `if __name__=="__main__"` 守卫；CSV 输出名不变。
- `strategy_common.py` → 断点读写迁入 `smcore/utils/checkpoint.py`（`load/save_checkpoint_df`+`merge_result_rows`）；Relativity 改从 `smcore.utils.checkpoint`/`smcore.utils.code` 导入。
- 删除：`Stock-Selection-Relativity.py`、`Stock-Selection-Ashare-Theme-Turnover.py`、`Stock-Selection-CCTV-Sectors.py`、`strategy_common.py`、`Stock-Selection-News.py`（全仓无引用的死脚本）。
- 调用方改写：`daily-pick.yml` 三步改 `python -m smcore.strategies.{theme,cctv,relativity}`；`auto_notify_boll.py` 删 `THEME/CCTV/RELATIVITY_SCRIPT_PATH` 常量、子进程改 `-m`（均 cwd=ROOT_DIR）；`boll-visualizer` 三个 UI + `test_cctv_sectors_strategy.py` 改动态加载新模块路径（_PARENT_DIR 指向项目根并插入 sys.path）。修复 theme.py 两 help 字符串字面量 `%` 触发 argparse 格式错误（原脚本预存 bug）。
- 提交 `0ca21be`（git mv 已暂存重命名，显式 `git add` 所有源文件+新建 checkpoint.py，排除 stock_data/db/daily_cache/.workbuddy）；push 成功 `0016632..0ca21be` → Render 重新部署。
- 验证：`py_compile` 全过；venv `import smcore.strategies` 四个 `run_*` 与 checkpoint 工具均存在；`python -m smcore.strategies.{theme,relativity,cctv} --help` 均 exit 0（boll 无 argparse，正常无参运行）。
