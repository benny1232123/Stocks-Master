# 2026-07-03 工作日志

## 数据源全面替换：东财 → 新浪
用户反馈 akshare 东方财富接口全部不可用，将网页应用所有 `_em` 接口替换为新浪源：

### 改动文件
1. **smcore/data/kline.py** — `_fetch_via_akshare()`: `ak.stock_zh_a_hist()`（东财）→ `ak.stock_zh_a_daily()`（新浪），symbol 格式从纯代码改为 sh/sz 前缀
2. **smcore/data/quote.py** — `_load_full_snapshot()`: `ak.stock_zh_a_spot_em()`（东财）→ `ak.stock_zh_a_spot()`（新浪）；`fetch_realtime_quotes()` 优先用 `quote_sina.py`（纯HTTP按需查询），失败回退全量快照
3. **smcore/data/index.py** — `fetch_index_close_series()`: 主源改为 `ak.stock_zh_index_daily()`（新浪），东财 `_em` 仅做最后回退
4. **smcore/data/quote_sina.py** — 新增 `fetch_sina_index_quotes()` / `fetch_sina_index_price()` / `_to_sina_index_symbol()`，支持指数行情（正确区分 sh000001 vs sz399001）
5. **pages/01_📊_看板.py** — 指数快照改用 `fetch_sina_index_quotes()`（新浪HTTP），市场热度改用 `ak.stock_zh_a_spot()`（新浪）
6. **pages/02_🔍_选股.py** — `get_candidate_codes()`: `ak.stock_zh_a_spot_em()` → `ak.stock_zh_a_spot()`，增加代码标准化处理新浪源带前缀的代码

### 验证结果
- 新浪HTTP指数行情：5个指数全部正常获取
- 新浪HTTP个股行情：600519/000001/002415 正常
- ak.stock_zh_a_daily（新浪K线）：600519 返回22条日线，数据正确
- ak.stock_zh_a_spot（新浪全市场）：5527只股票，列名与东财版一致
- 注意：新浪版 stock_zh_a_spot 代码列带 sh/sz/bj 前缀，需 format_stock_code 标准化

## auth 移除
- app.py 删除 check_auth() 调用
- pages/ 所有页面删除 from auth import check_auth
- auth.py 文件已删除
