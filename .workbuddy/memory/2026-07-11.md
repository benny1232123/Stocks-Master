# 2026-07-11 工作记录

## 00:4x 日报/回测三项问题修复收尾

用户反馈三个问题：① 日报点不进去；② 列表只要代码没名称；③ 一些回测曲线不动。全部修复并推送。

### 根因与修复
1. **回测重跑全部完成（23 个交易日曲线全部非平）**
   - 21 天为 top-N 完整回测；0623 重跑得 28 笔成交（曲线波动 98681~100855）；0702 为部分回测浮亏曲线（窗口未走完，持仓未平仓，末项 94747）。
   - 0623/0702 原平的根因**不是 K 线拉取失败**，而是"窗口未走完"——信号日距今近（9 天前），K 线缓存只到昨天，持仓到卖出日需 K 线到未来，故一直持有未平仓 → 浮亏曲线（真实有效）。
2. **日报"点不进去"**：日报页新增日期选择器。后端新增 `GET /api/artifacts/daily-action-list/dates`（返回 23 天列表）和 `?date=` 指定日查询；前端 report-head 加 `<select>` 切换，挂载时 `loadDailyDates()` 加载。
3. **只有代码没名称**：回填后期 CSV `股票名称` 为空（akshare 名称映射失败）→ 用 `scripts/fill_action_list_names.py` 补全所有日报 CSV 名称（验证 0 空）；后端 `/api/backtests/daily-latest` 的 trades 附带 `name`；前端交易明细/候选榜显示"代码+名称"。
4. **回测曲线不动**：
   - 根因 A：回填信号过多（0610 达 632 只），`max_positions=200` 把 10 万摊成数百个迷你仓位 → 曲线近水平。修复：`scripts/daily_backtest.py` 加 `TOP_N=30`，只取综合评分前 30 名参与回测，仓位有意义、曲线能动。
   - 根因 B：引擎 `end_pad = max_sig + hold_days + 15` 过度请求未来日期（如 07-27），akshare 对 07-11~07-27 拉取易限流**挂起**（0702 重跑卡 1m17s 被杀）。修复：`smcore/backtest/engine.py` 改 `end_pad = min(..., date.today() + timedelta(days=5))`，避免无谓远未来请求。
   - 前端：`EquityChart` 组件内加 `allFlat` 判断——曲线全部等于初始资金（真 0 成交）时显示"该信号日无成交标的"说明而非平线；调用处对 `trades===0` 但非平（部分回测）标注"⏳ 此信号日回测窗口尚未走完（持仓未平仓）"。

### 提交
- commit `27427f8`「fix(frontend+backtest): 修复日报/回测三项问题」已 push（`5d44444..27427f8`），Render 重建中。
- 临时脚本 `scripts/rerun_two_days.py` / `scripts/rerun_0702.py` / `scripts/fill_action_list_names.py` 一并入库。

## 01:42 日报四项数据/UI问题修复

用户截图反馈4个问题：
1. **有些没价格(¥--)**：Relativity/Theme策略42只股票缺买入价 → 用本地K线缓存补全（`_cache_path` + 最后收盘价）
2. **有些没名称(--)**：0710的5只Boll股票缺名称 → `fill_action_list_names.py` 补全
3. **日期选择器应做成复选框**：从 `<select>` 下拉改为 radio 复选框网格（MM-DD格式+信号数+选中高亮）
4. **策略分布"Relativity/"奇怪**：来源策略字段含尾部斜杠/空段 → split时 `.filter(Boolean)` 清洗

全部修复，commit `2ef6c6b` 已 push。新增脚本 `scripts/patch_daily_data.py`（通用日报CSV清洗工具）。

## 01:30 日报白屏真正根因：Number.isInfinity 不存在

用户截图 Console 显示 **`TypeError: Number.isInfinity is not a function`**，发生在 `Array.map`（DailyExpandableList 遍历行渲染时）。
- 我在盈亏比截断代码中用了 `Number.isInfinity(rr)` —— **这不是标准 JS API**。
- 正确写法是 `isFinite(rr)`（全局函数）或 `Number.isFinite()`。
- 修复：commit `c331759`，已 push。等 Render 重建后 `Ctrl+Shift+R` 刷新。

### 教训
- JS 中**不存在** `Number.isInfinity()`。检查无穷大用：
  - `isFinite(x)` → 返回 true 表示有限数
  - `Number.isFinite(x)` → 同上但不做类型转换
  - 直接比较 `x === Infinity || x === -Infinity`
- 写代码时不要"猜 API 名"，用已验证的。

## 01:08 日报"一片空白"根因修复

用户反馈：点击左侧「日报」tab 后右侧**完全空白**（不是"加载中"文字，是纯白）。

### 根因（最终确认）
**`fullDaily` 状态变量从未在应用启动时初始化**。它只在跑完"选股→融合"流程后才被 `reloadArtifacts()` 填充（原代码 line 628，融合完成回调）。如果用户从未跑过选股、或刷新页面后直接点日报 → `fullDaily === null` → 日报页条件渲染走空状态分支。

但为何是"空白"而不是"加载中..."？推测旧版线上可能存在构建/缓存问题导致连空状态都未渲染；或者用户看到的是浏览器缓存的更早期版本。

### 修复
1. **核心修复**（commit `7c43d85`）：挂载 effect 从 `useEffect(() => { loadDailyDates() }, [])` 改为 `useEffect(() => { loadDailyDates(); reloadArtifacts() }, [])`——应用启动即拉取最新日报数据。
2. **防御增强**（commit `09b07bd`）：日报空状态从单行文字升级为醒目卡片（📋 图标 + 标题 + 说明双行），杜绝任何"白屏观感"。
3. 两次提交均已 push（`07dded0..09b07bd`），Render 重建中。

### 经验
- React 页面"白屏"首查：状态依赖的数据是否在对应生命周期加载了。`useState(null)` + 只在特定回调里 `setState` = 首次进入该页必定为空。
- 空状态不能只靠一行小字——用户可能误解为"坏了"。用图标+标题+说明的卡片格式更安全。

### 经验（延续）
- akshare 拉未来日期必失败且易挂起，回测/选股脚本的 K 线请求区间必须限制到"今天+少量缓冲"，不要请求远未来。
- 回测"0 笔交易"要区分：真 K 线拉取失败（全平）→ 提示；窗口未走完（非平浮亏）→ 标注部分回测。两者前端呈现不同。

## 02:01 日报 UI 三项优化 + 定时任务提前

### 1. 日报日期选择器改为下拉框
用户反馈复选框网格太占空间 → 改为**一个方框 + 点击展开日期列表**的下拉选择器：
- 新增 `dateOpen` 状态控制展开/收起
- `date-dd-btn` 按钮显示当前选中日期（MM-DD 格式）
- `date-dd-menu` 下拉菜单列出全部 23 个可选日期，每项带信号数
- CSS 完全重写（`.date-dropdown / .date-dd-btn / .date-dd-menu / .date-dd-opt`）

### 2. 回测交易明细缺名称修复
截图显示 600323/600012 等 trades 表中名称为 "--"。根因：trades CSV 中代码无前导零（如 `'2354'`），后端名称查找用原码匹配日报的 `'002354'` → 失败。
- **修复**：`backend/main.py` 中代码先 `.zfill(6)` 补零再查 map，同时 fallback 原始码（`.lstrip("0")`），双重匹配确保命中。

### 3. 策略分布条被截断修复
"Relativity/" 后面内容被容器截断 → `.strat-row` grid 首列从固定 `72px` 改为 `minmax(80px, auto)` + `.strat-name` 加 `white-space: nowrap; overflow: hidden; text-overflow: ellipsis; min-width: 0`。

### 4. GitHub Actions 定时任务提前
`daily-pick.yml` cron 从 `"30 13 * * 1-5"` (北京时间 21:30) 改为 `"30 8 * * 1-5"` (北京时间 **16:30**)，收盘后约 1.5 小时自动跑。commit `7581294`。

### 提交
- commit `91fe46c`「fix(ui+backend): 日期选择器改下拉框 + 回测交易补名(代码补零) + 策略分布防截断」已 push。

## 02:16 日期选择器再改输入框式 + 0710名称nan修复

### 1. 日期选择器改为输入框样式
用户参考截图要求：一个方框显示 `2026/07/10` + 日历图标，点击展开选择。
- 完全重写 UI：`.date-picker-wrap > .date-picker-input(方框) + .dp-dropdown(3列网格)`
- 显示格式 `YYYY/MM/DD`（之前是 `MM-DD`）
- 下拉菜单为 3 列网格布局，每项带信号数 badge
- 加点击外部自动关闭（useEffect + mousedown 监听）

### 2. 0710 名称 "nan" 问题
**根因**：0710 CSV 中 600323/600012 的 `股票名称` 字段值为字符串 `"nan"`（pandas NaN 写出时变字符串）。
- **数据修复**：直接改 CSV，600323→瀚蓝环境、600012→皖通高速
- **前端防御**：DailyExpandableList 加 `displayName` 判断，将 `"nan"` / `"--"` / 空统一显示为 `--`

### 提交
- commit `0d9fd05` 已 push。

## 02:24 回测日期选择器改输入框式 + 名称问题确认

### 1. 回测「选择信号日」改为输入框样式
- 原 `dbt-tabs` 平铺标签（07-09 ·2天 / 07-08 ·5天 ...）→ 改为与日报页一致的 `.date-picker-input` 输入框（`2026/07/10` + 日历图标）
- 新增 `btDateOpen` 状态控制展开/收起
- 下拉菜单 4 列网格（回测有 ~23 个日期），每项带持有天数 badge

### 2. 名称 "--" 问题确认修复
- **数据层**：`Daily-Action-List-20260710.csv` 中 600323→瀚蓝环境、600012→皖通高速（原为字符串 "nan"）✅ 已验证
- **前端防御**：`DailyExpandableList` 加 `displayName` 过滤 `"nan"` / `"--"` → 统一显示 `"--"`
- **后端补名**：trades 查找时代码 zfill(6) 补零匹配

### 提交
- commit `99c6a9c` 已 push。

## 16:18 回测「总体总结」卡片 UI 全面升级

用户截图反馈总体总结 6 指标卡片视觉效果差 → 全面重设计：
- **新增图标**：每个指标配专属 Lucide 图标（TrendingUp/Down、Target、Percent、BarChart3、Trophy、AlertTriangle），带彩色圆角背景
- **布局重构**：从纯文字垂直堆叠 → 图标+文字横排（icon-left）
- **语义着色增强**：左侧 3.5px 色条 + 图标底色（绿/红/蓝/紫/琥珀五色区分）
- **最佳/最差日双栏**：从挤一行文字改为 Trophy+AlertTriangle 双栏并排，跨2列宽
- **网格固定化**：≥1024px 固定6列 / 移动端3列（替代 auto-fit minmax）
- **深度交互**：卡片悬浮 translateY(-2px)+阴影加深，外层渐变背景+双层阴影
- **字体层次**：label 加字距+500字重，val 700+tabular-nums 等宽数字

修改文件：`frontend/src/App.jsx`（新增6个Lucide导入+重写dbt-summary-grid JSX）、`frontend/src/styles.css`（重写全部.dbt-summary*样式 ~180→210行）。构建通过。

## 16:30 回测策略层根因修复（用户纠正：问题在策略非UI）

用户澄清"效果太差"指回测策略表现差，非界面。调查真实数据（已完成10日批次均值：总收益-0.6%、回撤-2.7%、胜率~41%、盈亏比≈1）。根因：①收盘止损被跳空击穿形同虚设（600226单日-23%）；②前向回测漏算佣金印花税；③汇总把0笔未走完批次当win_rate=0计入；④30只等权摊平；⑤cctv舆情为噪声却占15分。

落地修复（commit `d53b4d4` 已push，Render重建+CI重跑）：
- `engine.py`：加真实成本（佣金万2.5最低5元+卖出印花税千0.5）；新增gap-aware硬止损（盘中low触及-8%即按min(open,止损价)离场，封顶≈8%）
- `fusion.py`：新增趋势守卫（剔除价格低于MA20超12%的破位股，保留Boll轻度超卖票）；STRATEGY_BASE_SCORE cctv 15→10、relativity 25→30
- `backend/main.py`：daily-summary 剔除num_trades=0批次，胜率改按单笔交易统计

注：趋势守卫/重占比仅对新生成的操作清单生效（CI重跑selection附带）；硬止损/成本/汇总修正对历史批次回溯生效。下一步可选：加动量/相对强度新策略、跑各策略历史前向收益量化测量以数据定权重

## 16:40 动量策略开发 + 量化测量（用户"继续"）

### 新增动量/相对强度策略（第5策略，买强不买弱，与Boll超卖互补）
- 新建 `smcore/strategies/momentum.py`：东财全市场快照预筛（价5~30/成交额≥2亿/换手率≥1%/60日涨幅≥0）→ 取Top80拉前复权K线确认动量（ret20>0且MA20上行才过，量能确认）→ 输出 `Stock-Selection-Momentum-{today}.csv`（含动量分/20日收益%/60日收益%/MA20斜率%/距20日高点%）。
- 评分权重 W_RET20=0.40/W_RET60=0.30/W_MA20_SLOPE=0.30（修复：原ma20_slope误用W_RET20）。
- 接入：`smcore/strategies/__init__.py`（导出run_momentum）、`smcore/strategy/fusion.py`（`_load_momentum_picks` + 打分 + 日报 + trend_guard过滤）、`smcore/strategy/allocation.py`（三regime均加momentum权重：趋势上行20/下行防御5/震荡15）、`.github/workflows/daily-pick.yml`（策略5步骤 continue-on-error + 产物上传）。

### 量化测量各策略历史edge（数据驱动重定占比）
- 新建 `scripts/measure_strategy_edge.py`：读24个Daily-Action-List(0606起)，按来源策略拆桶，预拉K线缓存后跑改进引擎前向10日回测，聚合各策略 avg_return/win_rate/drawdown/sharpe/trades + BASELINE对照。
- 后台运行中（task PLQV4x，沙箱E:\Anaconda\python.exe，输出 stock_data/measure_edge.log）。沙箱stdout缓冲，结束才落盘。
- 注意：测量只覆盖现有4策略（无历史momentum数据），故momentum权重(暂25)仍属推理，待测量后按relativity(含强度维度)的edge印证。

### 待办
- 等 measurement 完成 → 用真实10日收益定稿 STRATEGY_BASE_SCORE（尤其boll/relativity/theme/cctv）+ allocation 权重 → 提交推送全部策略层改动。
- momentum 权重最终定稿（先25，看relativity edge是否支持"买强"维度）。

## 17:42 东财接口不可用 → 全链路去掉东财依赖

用户反馈"东财接口用不了"。排查后确认：项目 K 线后端 `fetch_daily_k` 的 akshare 分支其实走**新浪** `stock_zh_a_daily`（东财-free），只有以下处真用东财：
- `momentum.py` 快照 `ak.stock_zh_a_spot_em`（东财）→ 改为**新浪 `ak.stock_zh_a_spot`**（东财-free，5529行，~20s，偶发返回空已加3次重试）；东财仅 `MOMENTUM_USE_EASTMONEY=1` 时回退尝试（15s线程超时防挂）。新浪快照无 换手率/60日涨跌幅列 → 预处理去掉换手率门槛、60日门槛移入 K 线确认步骤（`_momentum_metrics` 现在要求 ret60>=min_60d）。
- `cctv.py` `_next_day_return` 用东财 `stock_zh_a_hist` → 改为 `fetch_daily_k`（东财-free，baostock/新浪）。
- `measure_strategy_edge.py` 原 `KLINE_BACKEND=akshare` 在本地导致每只股票 ~60s 超时重试（akshare分支挂起）才回退 baostock，整体卡死 → 改为 `KLINE_BACKEND=baostock`（直连快，CI 不可达时 fetch_daily_k 自动回退新浪）。已杀掉原卡死进程(PID 57732)，用 baostock 后端重跑（后台 task oUF8Ub，unbuffered 日志 stock_data/measure_edge.log）。

### 验证
- baostock K线 OK（600276 47行/5.8s）；新浪快照 OK；腾讯 gtimg 单码 OK（0.06s，项目已用于市场宽度）。
- momentum 实测产出 10 只（京基智农/众生药业/新亚强/贝肯能源…）并落 `Stock-Selection-Momentum-20260711.csv`，全程无东财调用。
- 注意：baostock `query_all_stock(day=今天)` 返回 0（日期参数不稳），故未采用"baostock列表+gtimg报价"备选，保留新浪快照为主。

## 18:55 测量完成 + 占比定稿 + 出场逻辑改进

### 第一次测量结果（窗口 0606-0626，硬止损-8%+真实成本，已落 stock_data/measure_edge.log）
- Boll 收益-5.48% 胜率29.3% 回撤-8.64% 夏普-3.46 交易150（最抗跌）
- Relativity 收益-9.57% 胜率39.2% 回撤-13.86% 夏普-2.62 交易79（最差、集中度高）
- Theme/CCTV 批次=0（窗口内无样本：这两类信号0628后才出现，测量只收距今≥15天窗口即≤0626，自然排除）
- BASELINE -6.37% 胜率33.3% 回撤-7.81% 夏普-6.72 交易159
- 关键结论：**之前那轮修复是让回测变诚实，不是变赚钱**；该窗口所有策略负期望，Boll最抗跌。胜率29-39%说明"买后持有10天"盈亏结构差，真实杠杆在出场逻辑。

### 占比定稿（按实测edge，已提交 2975150，未push）
- STRATEGY_BASE_SCORE: boll 40→45(提权为锚), relativity 30→15(砍权), momentum 25→20(适度), theme 20→15(谨慎), cctv 10(不变)。
- allocation 三regime: Boll全面提权、Relativity全面砍权、Momentum适度、Theme/CCTV谨慎；normalize兜底同步。
- 注意：在亏损窗口里挪仓只是少亏不是扭亏；Theme/CCTV权重仍属谨慎(无样本)，momentum权重属推理(无历史数据)。

### 出场逻辑改进（引擎+测量，正在重测验证）
- `smcore/backtest/engine.py` `run_forward_signal_backtest` 新增 `trend_exit_ma` 参数 + `_ma_n` 辅助：收盘跌破 N 日均线即 `trend_break` 离场（N=60 时 Boll 买点(近MA20、远高于MA60)不会误触发）。
- 测量 `run_set` 改用：take_profit_pct=0.06(固定止盈锁利) + trailing_stop_pct=0.05(原0.08太宽) + trend_exit_ma=60。
- 烟测2只(600276/000001, 0610信号)已正向(+0.98% 胜率50% 夏普1.06)，但属个例。全量重测后台 task IyI2iY，日志 stock_data/measure_edge_v2.log，约40分钟。
- 重测完成(task IyI2iY, 日志 measure_edge_v2.log)，已提交(1959ccc)。v2 vs v1(硬止损8%+移动8%)对比：
  - BASELINE(全信号, 最贴近融合回测): -6.37%→-5.09%(+1.28pct), 回撤 -7.81%→-6.69% 收窄 → 净改善
  - Boll 主策略: -5.48%→-4.92% 改善
  - Relativity 单策略: -9.57%→-13.86% **恶化**(胜率39→48但收益更差、回撤-13.86→-16.41扩大；MA60破位把亏损单赶在反弹前割+移动止盈5%截利润，对高波动相对强度票不利)
  - 结论：整体净改善(少亏)，Relativity恶化但已砍权(15分)且全样本净正，可接受。
- 已同步生产回测 daily_backtest.py 到新出场(take6%+trailing5%+MA60break)，UI回测一并受益(exit_mode标签更新)。
- 下一步真实杠杆：① per-strategy 自适应出场(Relativity关MA60破位，避免其恶化)；② 市场/指数趋势闸门(弱势不买均值回归，根因在信号侧)。当前仍是"少亏非扭亏"——窗口0606-0626偏弱市，所有策略负期望。

### 趋势闸门（市场择时防御）2026-07-11
- `smcore/strategy/fusion.py` `fuse_signals` 新增 `market_gate` 参数 + `_detect_market_regime()`：用沪深300 MA60位置判市场状态(趋势上行/下行防御/震荡轮动)，下行防御时剔除纯均值回归候选(仅Boll/Relativity)、驱动 allocation 走防御regime。新浪指数 sh000300(东财-free, 0.6s)。
- 已用临时输入验证：强制下行防御时纯Boll/Relativity被剔除、Theme/Momentum保留；日报标注。已提交(04e460c)+README说明(4ab7947)+push。
- **关键根因洞察**：0606-0626亏损窗口沪深300实际趋势上行(13天中11天)，趋势闸门在该窗口几乎不触发 → 那轮亏损根因是**策略alpha弱(大盘涨、个股策略仍亏)**而非市场弱。趋势闸值是防御层(未来真熊市有用)，非该窗口灵药。
- 真正杠杆在alpha质量(个股趋势/相对强度)：下一步可做 ① 融合层加"个股相对大盘强度"过滤(剔除近20日跑输沪深300的超卖票)；② 升级Boll选股质量(不只看超卖、要求企稳/放量)。需重测验证(缓存命中则快)。
- 注：relativity恶化(MA60破位对其不利)的per-strategy出场仍未做，属已知待办。
