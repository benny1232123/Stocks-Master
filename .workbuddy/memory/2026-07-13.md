# 2026-07-13 工作记录

## 市场自适应四方向（续，前一日 2026-07-12 完成代码）
- 06 个本地 commit（`a5907ca` README流程/`4126631` 状态化评分/`0bf1b79` 仪表盘+动态阈值/`3687b24` 波动率风控/`4f6e3c3` 板块轮动+集中度/`819aa4d` memory）已于本日推送 `origin/master`（`56d4930..819aa4d`），Render 重新部署。
- 云端当前 fail-soft：缺 `stock_data/sector_map.json` 时 `get_sector_map()` 因 `SECTOR_MAP_ALLOW_BUILD` 默认 False 返回空 → `apply_sector_cap` 对"未知"行业豁免 → 板块控制暂不生效，不崩。

## 板块行业映射构建（仍在进行）
- `scripts/build_sector_map.py` 后台用 baostock `query_stock_industry` 抓全市场 5031 只 A 股行业，断点续跑、每 200 只落盘 `stock_data/sector_map.json`。
- 启动于 2026-07-12 16:23 UTC（约 6.5min→200 只），预计 ~2 小时完成。
- 完成后需：① 提交 `stock_data/sector_map.json`（已确认 `.gitignore` 不忽略它，会被跟踪）；② `git push origin master` 触发 Render 重新部署 → 板块轮动+集中度首次完整生效。
- 安全闸：模块 `_build_and_cache` 仅当 `SECTOR_MAP_ALLOW_BUILD=1` 才联网抓，云端绝不自动触发（防 CI 卡 80min）。

## 待办
- [ ] 等 AKydu4 构建完成通知 → 提交 sector_map.json → 推送 → 验证云端 `get_sector_map()` 命中行业、日报出现板块行。
