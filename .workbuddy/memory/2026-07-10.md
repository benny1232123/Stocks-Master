# 2026-07-10 工作日志

## 前端 UI 修复（6 处截图反馈）
- 修复概览 Hero 日报预览价格显示错误：改为显式列名读取（`股票代码`/`股票名称`/`最新价`），避免把「命中策略数」显示成价格 1.00。
- 选股页合并「开始选股」与「运行完整布林选股(多因子)」两个按钮为单个「开始选股」，串联完整流程：候选池 → 完整布林多因子 → 策略融合 → 自动回测。
- 个股分析页：移除左侧鸡肋列表，改为顶部可搜索下拉选择器 + 代码输入框。
- 日报/最新日报：改为二层可展开界面（`DailyExpandableList` 组件），首层显示代码/名称/来源策略/评分/买入价，展开显示仓位/金额/止损/止盈/MA20。
- 融合完成后前端自动刷新 `/api/artifacts/daily-action-list` 与 `/api/artifacts/daily-action-list/full`，确保 Overview 与日报页展示最新结果。

## 提交
- commit `0d23cb5` push 成功（`1e951bc..0d23cb5 master -> master`），触发 Render 重新部署。
- 改动文件：`frontend/src/App.jsx`、`frontend/src/styles.css`。

## 待跟进
- Render 部署成功后验证 UI 实际效果。

## 个股分析页指标可视化改造
- **删除无用 select 下拉框**：个股分析页顶部 `<select>`（依赖 `analysisList`，经常为空不渲染）已移除，仅保留代码输入框 + 加载按钮。
- **MACD 可视化**：新增零轴柱状图（红/绿柱表示正负柱状值，蓝点 DIF / 黄点 DEA 标记在零轴两侧位置）。
- **KDJ 可视化**：新增三线仪表盘（0–100 刻度，超卖绿/中性灰/超买红三区，K(蓝)/D(黄)/J(红) 三根指针；J 超出范围时高亮发光）。
- **均线系统可视化**：新增梯形横向条形图，MA5/10/20/60 按价格高低排序显示为水平条（每条独立颜色），一眼看出排列关系。

## 每日全策略清单自动接入回测系统
- 新增 `scripts/daily_backtest.py`：CI 生成每日操作清单后，读取最新 `Daily-Action-List-*.csv`，提取股票代码与来源策略，用近 90 天 K 线跑 `run_multi_strategy_backtest`，结果存 `stock_data/Multi-Backtest-{date}-{summary,equity,trades}.csv`（equity 补算 peak/drawdown 列供前端回撤图）。
- `daily-pick.yml` 新增「每日清单多策略回测」step（continue-on-error, timeout 40min），提交步骤加 `git add -f stock_data/Multi-Backtest-*.csv`。
- `.gitignore` 加白名单 `!stock_data/Multi-Backtest-*.csv`（与 Daily-Action-List 同机制）。
- 后端 `backend/main.py` 新增 `GET /api/backtests/daily-latest`，读最新 `Multi-Backtest-*-summary.csv` + equity/trades 返回前端。
- 前端回测页顶部新增「每日自动回测」卡片（总收益/回撤/胜率/夏普/权益曲线/交易明细），挂载时自动加载 `loadDailyBacktest()`。
- 本地实跑验证通过：15 只、区间 2026-04-11~2026-07-10、策略 boll,relativity,theme，23 笔，总收益 -0.1%、回撤 -5.53%、胜率 30.4%。
