# 2026-06-28 工作日志

## Stocks-Master 重构（阶段1：内核 + 止血）

### 诊断
- "不可信"头号根因：命令行 `Stock-Selection-Boll.py` 用不复权(adjustflag=3)算 Boll，可视化用前复权(qfq)——同一只票两套信号；不复权数据遇除权除息日布林带断裂、信号失真。
- Boll 逻辑 3 套独立实现：`Stock-Selection-Boll.py` / `visualizer/core/indicators.py` / `auto_notify_boll._calc_boll_levels`。
- `auto_notify_boll.py` 3306 行巨石，塞了 10 件事；两条主线零代码共享；缓存 SQLite+CSV 双轨；baostock login 散落 6+ 处。
- 明确 bug：`run_macro_news.py:44` 漏 import os。

### 完成
- 建立 `core/` 共享内核：utils（代码标准化统一4处、财报日期、日志）、indicators/boll（唯一实现）、config/defaults（统一参数，强制前复权，股价上限统一30）、data/session（baostock 单例）、data/kline（前复权K线+缓存）。
- 修复致命 bug：`Stock-Selection-Boll.py` adjustflag 3→2；`run_macro_news.py` 补 import os。
- 验证：py_compile + import + Boll 超卖信号行为测试全通过。
- 进度文档：`REFACTOR_PROGRESS.md`。

### 环境备忘
- 运行环境：`/e/Anaconda/python.exe`（含 pandas/akshare/baostock/streamlit）；managed python 3.13 无项目依赖，做验证需用 anaconda。
- 任务1（core 骨架）、任务2（bug 修复）已 completed；任务3/4/5 pending。

### 阶段 2-A：可视化主线迁移（任务4 完成）
- 内核包改名 `core/` → `smcore/`（避免与 visualizer 的 src/core 同名冲突）。
- `streamlit_app.py` 把仓库根加入 sys.path，使 smcore 可见。
- visualizer 的 `core/indicators.py`、`utils/config.py`、`core/data_fetcher.py`(K线+代码标准化) 改 re-export smcore。
- 验证：boll_strategy / full_flow_strategy / backtester 的 Boll 全部经 smcore，无独立实现；股价上限统一 30。
- `app import` 失败于 protobuf 版本冲突（supabase 依赖），是 anaconda 环境问题，非重构引入。
- 任务4 completed。任务3（拆 auto_notify_boll 3306行巨石，高危）、任务5 待办。

### 待办（阶段2）
- 任务3 剩余：3b 宏观风险抽出（~800行，NLP/经济日历/美股外汇期货 → smcore/risk）、3d 消息构建/归档抽出。
- 任务5：统一缓存、行为对照测试、清理 Unnecessary-Programs、更新 README。

## 2026-06-29 补充

### 阶段 2-B：命令行主线迁移（任务3，3a/3c 完成）
- **3a Boll/数据委托（"不可信"根因修复）**：`auto_notify_boll.py` 第三套 Boll 实现 `_build_indicator_levels` 重写为委托 smcore（前复权 fetch_daily_k + calc_bollinger + session 单例）；`_fetch_bs_latest_row`/`_fetch_bs_close_series` 的 `adjustflag="3"`→`"2"`；`_normalize_code`/`_to_bs_code`/`_to_ak_index_symbol` 委托 smcore.utils.code。
  - 等价性验证：代码标准化 8 用例全等；Boll 水位（ma20/upper/lower）误差 <1e-9。
  - 真实数据冒烟：600519/000001/601318 三票经 baostock 返回合理水位（lower<ma20<upper）。
- **3c 推送抽出**：新建 `smcore/notify/`（wecom.py + email.py）；巨石 `send_wecom_markdown`/`send_email` 改委托，签名不变，3 处调用方零改动。
- 巨石 3306→3240 行。核心"不可信"已治；剩余 3b/3d 为代码组织优化（非 bug 修复）。
- 任务3 仍 in_progress（3b/3d 待办）。
