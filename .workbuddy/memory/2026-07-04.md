# 2026-07-04 工作日志

## Render 保活机制
- 创建 `.github/workflows/keep-alive.yml`：每 10 分钟用 curl ping Render URL，防止免费版 15 分钟休眠
- 需要在 GitHub Secrets 配置 `RENDER_URL`（值为 Render 应用的完整 URL）
- 零费用，无需注册第三方服务（替代 UptimeRobot 方案）

## 清理无用代码/文件
- 删除：`smcore/notify/wecom.py`（企业微信）、`smcore/scheduler/`（调度器引擎+任务）、`run_daemon.py`（24h守护）、`run_macro_news.py`（宏新闻调试脚本）、`scripts/`（17个bat定时脚本）、`streamlit_app.py`（旧单页入口）、`rules.txt`、`requirements-dev.txt`、项目级 `__pycache__/`
- 保留：`smcore/storage/cos.py`（GitHub Actions 仍用）、`smcore/notify/email.py`（GitHub Actions 仍用）、`Frequently-Used-Program/`（GitHub Actions 仍引用 auto_notify_boll.py）
- 验证：所有网页应用核心模块导入正常
