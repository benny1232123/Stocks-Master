"""Stocks-Master 共享内核。

两条主线（命令行 auto_notify_boll + 可视化 boll-visualizer）的单一真相源。
所有数据获取、指标计算、策略、风控、回测、通知都在这里，入口层只做薄封装。

重构动机：此前 Boll 逻辑有 3 套独立实现、代码标准化有 4 处、参数两套值、
复权方式冲突（命令行不复权 / 可视化前复权），导致结果不可信。本包统一消除分歧。
"""
