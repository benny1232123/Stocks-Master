# k_data 前复权基准全量重拉报告

日期：2026-09-14 ｜ 执行时长：59 分钟（21:06–22:05）｜ 状态：**已完成、已校验、未提交**

---

## 1. 为什么重拉

`stock_data/k_data/*.parquet`（全库日线前复权缓存）的历史段已损坏，且**在 HEAD 提交版里同样损坏**
（`git show HEAD:...` 抽样 `000001` 逐位相同 → 不是 09-13 本地扩量引起，是 2026-09-09 parquet 迁移
`1afadb32` 把已坏的 CSV 一并搬过来了）。

三源交叉验证铁证（`000001` 平安银行）：

| 来源 | 2015-01-05 收盘 |
|---|---|
| 旧缓存 | **0.0426** |
| 同行 `amount/volume`（真实均价） | 15.96 → 隐含因子 374x、11 年 +27792% |
| baostock 前复权 | 8.1886 |
| hithink 前复权 | 7.6037 |

失真形态是 `缓存 = 真值 × g(t)`（`g` 平滑单调、`g(最后更新日)=1`），**不是跳变** →
现有两层守卫都抓不到：① `_detect_adjust_drift` 只比对本次请求段；② `find_price_breaks` 只抓相邻日跳变。

重拉还额外发现：旧缓存在 **b30 / b68 两个桶里有大段缺失**（每票仅 ~190 行 ≈ 9 个月）。

---

## 2. 实现方案

脚本：`.workbuddy/repull_kdata_hithink.py`（可重入 / 断点续跑 / 每物理文件只写一次 / 原子替换）

### 2.1 先做实测，再定参数

| 实测 | 结论 |
|---|---|
| 个股 K 端点跨度上限 | 单次 `start` 距 `end` > **~3652 自然日**即整段静默返空（3652d ✅ / 4017d ❌）→ 按 **1500 天分片** |
| 复权基准是否随窗口重锚 | **不重锚**：同区间不同窗口，重叠日 close 比值**恒 = 1.0（cv=0）**，qfq/hfq 皆然 → **分片拼接无 seam** |
| hithink qfq 近期可靠性 | 2026-09-11 收盘与 akshare **完全相同**；近 250 日收益最大差 0.17–0.33pp |
| hithink qfq 深历史 | **加法式失真**；高分红长历史股出**负价**（600519 369 行 min −143.58 / 000858 294 / 000333 254 / 000651 248 / 600036 61） |
| hithink hfq 能否反推 qfq | **不能**：`hfq/hfq[-1]` 对 000002 近 250 日单日收益差 6.56pp（而其 qfq 与 akshare 一致） |
| akshare(新浪) | 标准乘法前复权、非正价 0 行、0.82s/只 → 定为二级兜底 |

### 2.2 源阶梯（每只股票）

`T1 hithink qfq` →（序列出现任何 `close<=0`）`T2 akshare qfq` →（不可用）`T3 hithink hfq 派生`
→（全失败）`T4 保留旧行`（维持 universe 不变）。

---

## 3. 结果

| 物理文件 | 行数（旧 → 新） | 大小（旧 → 新） |
|---|---|---|
| `qfq_b00.parquet` | 3,548,874 → **3,697,209** | 100.1 → 95.1 MB |
| `qfq_b30.parquet` | 158,531 → **1,544,939**（**10x**） | 3.8 → 44.0 MB |
| `qfq_b60_0.parquet` | 1,973,120 → **2,046,079** | 51.9 → 51.0 MB |
| `qfq_b60_1.parquet` | 523,790 → **536,519** | 14.8 → 14.1 MB |
| `qfq_b60_3.parquet` | 1,195,720 → **1,240,439** | 39.4 → 35.8 MB |
| `qfq_b60_5.parquet` | 147,966 → **154,404** | 6.0 → 4.7 MB |
| `qfq_b68.parquet` | 68,111 → **412,600**（**6x**） | 1.7 → 12.9 MB |
| **合计** | 7,616,112 → **9,632,189** | — |

- 股票数：**4380**（与旧库一致，universe 未变）
- 源分布：**hithink 4310（98.4%）／ akshare 63（1.4%）／ legacy 7（0.16%）**
  - legacy（hithink 与 akshare 都取不到，多为 ST/退市/长期停牌）：
    `000004 002808 002898 301655 600193 600608 605081`
- **所有文件 < 100 MiB**（b00 95.1 MB）→ 不触发 GitHub GH001 单文件硬限

---

## 4. 校验

### 4.1 非正价
**全库 `nonpos = 0`**（7 文件 / 963 万行零非正价）✅

### 4.2 与标准前复权（akshare 新浪）逐日收益对比

| 窗口 | 新数据（中位 / 最差） | 旧数据（中位 / 最差） |
|---|---|---|
| **近 250 交易日** | **0.061 pp / 0.721 pp** | 0.663 pp / **291.15 pp** |
| 全历史 | **1.475 pp / 17.8 pp** | **220.2 pp / 845.6 pp** |

→ 近期窗口（所有线上信号实际用到的区间）单日误差从 **291 pp 降到 0.72 pp**。
→ 全历史仍有 1.5–18 pp 偏差 = hithink qfq 深历史的加法式失真，**已知特性**；若要长历史严格标准口径，须整库改用 akshare 重建。

### 4.3 完整性
- 全库最大空洞 1518 天（`000029` 深深房A 真实长停牌）。
- 空洞 >20d 1617 只 / >60d 1204 只 / **>250d 69 只** → 对 69 只**全部**与 akshare 比交易日集合：
  **`missing_in_new = 0`，REAL-MISSING count = 0** → 全部是真实停牌 ✅

### 4.4 抓到并修掉一个真缺陷
4 只股票（`002533 300189 600644 603033`）末日期**卡在分片边界 2023-03-21** —
hithink 对某一段**偶发静默返空**，原脚本跳过后"非空+正价"判据仍通过 → **悄悄丢尾段**。
⇒ 新增 `.workbuddy/repair_kdata_codes.py`（空分片重试 3 次）已修复 4 只（各 +~850 行至 2026-09-14），
同一重试逻辑已回补进主脚本。**终检 `stale = 0`**。

---

## 5. 代码改动

| 文件 | 改动 |
|---|---|
| `smcore/data/kline.py` | `write_kline_cache` 新增**非正价硬防线**：`close<=0` 即 stderr 告警并**拒绝写入**（不变量，无魔数） |
| `tests/test_walk_forward.py` | `test_sweep_returns_all_configs` 改为**容差带守卫** `max(diff) > -sweep_edge_tol_pp`（默认 2.5），保留结构断言 |
| `smcore/strategy/adaptive_weights.py` | 内置默认新增 `sweep_edge_tol_pp: 2.5` |
| `smcore/strategy/adaptive_weights_config.json` | 同步新增 `sweep_edge_tol_pp: 2.5` |

### 回归
- `tests/test_walk_forward.py` → **7 passed**（重建后的数据上跑，exit 0）
- `tests/test_kline_guard.py` → **5 passed**
- `stock_data/fundamental_cache/` → `git status` **干净**（无测试污染）

---

## 6. 备份与后续

- 备份：`.workbuddy/k_data_backup_20260914/k_data/`（224.1 MB / 7 文件，重拉前原样）
- ⚠️ **未提交**。共 7 个 k_data 文件改动（净 +39.9 MB），7 个桶全部变化。
  推送建议：单包 <~70 MB，用 SSH-via-HTTP-CONNECT 隧道；提交后立即 push，不留过夜。
- 遗留（非阻塞）：
  1. 新缓存全历史仍有 1.5–18 pp 跨源偏差（hithink qfq 深历史失真）。如需长历史严格标准口径 → 整库改 akshare/baostock 重建。
  2. 全库未做 akshare 全量完整性比对（本次只对空洞 >250d 的 69 只做了，全部通过）。
  3. `_detect_adjust_drift` 仍只覆盖"本次请求段"；本次加的是**落盘出口**硬防线（非正价），
     对"平滑累积缩放"这类失真仍无在线检出 → 可考虑加一条「`(amount/volume)/close` 随时间的平滑性」巡检。
